class AppConstants {
  AppConstants._();

  // Shared Preferences keys
  static const String themeModeKey = 'theme_mode_key';

  // API Configuration
  static const String spoonacularApiKeyEnvName = 'SPOONACULAR_API_KEY';

  // UI layout configurations
  static const double defaultPadding = 25.0;
  static const double borderRadius = 12.0;
}
